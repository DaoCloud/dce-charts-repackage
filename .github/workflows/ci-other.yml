name: PR other E2E

on:
  pull_request:
    paths:
      - "test/*"

jobs:
  call_e2e:
    uses: ./.github/workflows/call-e2e.yml
    with:
      project: "spiderpool"
      ref: ${{ github.event.pull_request.head.sha }}
    secrets: inherit
