- "{{ .image.registry }}/{{ .gpuLabel.image.name }}:{{ .gpuLabel.image.version }}"
- "{{ .image.registry }}/{{ .driver.image.name }}:{{ .driver.image.version }}"
- "{{ .image.registry }}/{{ .driver.payload.name }}:{{ .driver.payload.version }}"
- "{{ .image.registry }}/{{ .maca.image.name }}:{{ .maca.image.version }}"
- "{{ .image.registry }}/{{ .runtime.image.name }}:{{ .runtime.image.version }}"
- "{{ .image.registry }}/{{ .gpuDevice.image.name }}:{{ .gpuDevice.image.version }}"
- "{{ .image.registry }}/{{ .dataExporter.image.name }}:{{ .dataExporter.image.version }}"
- "{{ .image.registry }}/{{ .controller.image.name }}:{{ .controller.image.version }}"
- "{{ .image.registry }}/{{ .maca.payload.name }}:{{ .maca.payload.version}}"