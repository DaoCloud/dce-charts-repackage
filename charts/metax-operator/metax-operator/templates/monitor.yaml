# Prometheus Monitor Service (Metrics)
# install prometheus_opertor before uncommenting

# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   labels:
#     app.kubernetes.io/name: service-monitor
#   name: gpu-operator-metrics-monitor
#   namespace: {{ .Release.Namespace }}
# spec:
#   endpoints:
#     - path: /metrics
#       port: https
#       scheme: https
#       bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
#       tlsConfig:
#         insecureSkipVerify: true
#   selector:
#     matchLabels:
#       app.kubernetes.io/name: metax-operator
