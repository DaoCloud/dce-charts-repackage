- "{{ .jenkins.registry }}/{{ .jenkins.Master.Image }}:{{ .jenkins.Master.ImageTag }}"
- "{{ .jenkins.registry }}/{{ .jenkins.Agent.Image }}:{{ .jenkins.Agent.ImageTag }}"
- "{{ .jenkins.registry }}/{{ .jenkins.Agent.Builder.Base.Image }}:{{ .jenkins.Agent.Builder.Base.Tag }}"
- "{{ .jenkins.registry }}/{{ .jenkins.Agent.Builder.NodeJs16.Image }}:{{ .jenkins.Agent.Builder.NodeJs16.Tag }}"
- "{{ .jenkins.registry }}/{{ .jenkins.Agent.Builder.Maven.Image }}:{{ .jenkins.Agent.Builder.Maven.Tag }}"
- "{{ .jenkins.registry }}/{{ .jenkins.Agent.Builder.Golang.Image }}:{{ .jenkins.Agent.Builder.Golang.Tag }}"
- "{{ .jenkins.registry }}/{{ .jenkins.Agent.Builder.Python.Image }}:{{ .jenkins.Agent.Builder.Python.Tag }}"
- "{{ .jenkins.trace.image.registry }}/{{ .jenkins.trace.image.repository }}:{{ .jenkins.trace.image.tag }}"
- "{{ .jenkins.eventProxy.image.registry }}/{{ .jenkins.eventProxy.image.repository }}:{{ .jenkins.eventProxy.image.tag }}"
