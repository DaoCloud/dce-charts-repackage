- "{{ .jenkins.image.registry }}/{{ .jenkins.Master.Image }}:{{ .jenkins.Master.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Image }}:{{ .jenkins.Agent.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Base.Image }}:{{ .jenkins.Agent.Builder.Base.ImageTag }}-podman"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Base.Image }}:{{ .jenkins.Agent.Builder.Base.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.NodeJs16.Image }}:{{ .jenkins.Agent.Builder.NodeJs16.ImageTag }}-podman"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.NodeJs16.Image }}:{{ .jenkins.Agent.Builder.NodeJs16.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Maven.Image }}:{{ .jenkins.Agent.Builder.Maven.ImageTag }}-podman"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Maven.Image }}:{{ .jenkins.Agent.Builder.Maven.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Mavenjdk11.Image }}:{{ .jenkins.Agent.Builder.Mavenjdk11.ImageTag }}-podman"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Mavenjdk11.Image }}:{{ .jenkins.Agent.Builder.Mavenjdk11.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Golang.Image }}:{{ .jenkins.Agent.Builder.Golang.ImageTag }}-podman"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Golang.Image }}:{{ .jenkins.Agent.Builder.Golang.ImageTag }}"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Python.Image }}:{{ .jenkins.Agent.Builder.Python.ImageTag }}-podman"
- "{{ .jenkins.image.registry }}/{{ .jenkins.Agent.Builder.Python.Image }}:{{ .jenkins.Agent.Builder.Python.ImageTag }}"
- "{{ .jenkins.trace.image.registry }}/{{ .jenkins.trace.image.repository }}:{{ .jenkins.trace.image.tag }}"
- "{{ .jenkins.eventProxy.image.registry }}/{{ .jenkins.eventProxy.image.repository }}:{{ .jenkins.eventProxy.image.tag }}"