- "{{ .sonarqube.ingress-nginx.controller.image.registry }}/{{ .sonarqube.ingress-nginx.controller.image.image }}:{{ .sonarqube.ingress-nginx.controller.image.tag }}"
- "{{ .sonarqube.ingress-nginx.controller.admissionWebhooks.patch.image.registry }}/{{ .sonarqube.ingress-nginx.controller.admissionWebhooks.patch.image.image }}:{{ .sonarqube.ingress-nginx.controller.admissionWebhooks.patch.image.tag }}"
- "{{ .sonarqube.initContainers.registry }}/{{ .sonarqube.initContainers.repository }}:{{ .sonarqube.initContainers.tag }}"
- "{{ .sonarqube.initSysctl.registry }}/{{ .sonarqube.initSysctl.repository }}:{{ .sonarqube.initSysctl.tag }}"
- "{{ .sonarqube.caCerts.registry }}/{{ .sonarqube.caCerts.repository }}:{{ .sonarqube.caCerts.tag }}"
- "{{ .sonarqube.prometheusExporter.registry }}/{{ .sonarqube.prometheusExporter.repository }}:{{ .sonarqube.prometheusExporter.tag }}"
- "{{ .sonarqube.plugins.registry }}/{{ .sonarqube.plugins.repository }}:{{ .sonarqube.plugins.tag }}"
- "{{ .sonarqube.image.registry }}/{{ .sonarqube.image.repository }}:{{ .sonarqube.image.tag }}"
- "{{ .sonarqube.postgresql.volumePermissions.image.registry }}/{{ .sonarqube.postgresql.volumePermissions.image.repository }}:{{ .sonarqube.postgresql.volumePermissions.image.tag }}"
- "{{ .sonarqube.postgresql.metrics.image.registry }}/{{ .sonarqube.postgresql.metrics.image.repository }}:{{ .sonarqube.postgresql.metrics.image.tag }}"
- "{{ .sonarqube.postgresql.image.registry }}/{{ .sonarqube.postgresql.image.repository }}:{{ .sonarqube.postgresql.image.tag }}"