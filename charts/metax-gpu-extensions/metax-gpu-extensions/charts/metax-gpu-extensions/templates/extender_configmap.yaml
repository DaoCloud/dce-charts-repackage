{{- if .Values.deployExtenderGPUAware }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: gpu-aware-weight
  namespace: {{ include "GPUExt.namespace" . }}
data:
  score: "{{ .Values.gpuAware.scoreWeight }}"
  loss: "{{ .Values.gpuAware.lossWeight }}"
{{- end }}