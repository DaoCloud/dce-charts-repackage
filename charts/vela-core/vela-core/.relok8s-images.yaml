- "{{ .vela-core.image.registry }}/{{ .vela-core.image.repository }}:{{ .vela-core.image.tag }}"
- "{{ .vela-core.multicluster.clusterGateway.image.registry }}/{{ .vela-core.multicluster.clusterGateway.image.repository }}:{{ .vela-core.multicluster.clusterGateway.image.tag }}"
- "{{ .vela-core.test.app.registry }}/{{ .vela-core.test.app.repository }}:{{ .vela-core.test.app.tag }}"
- "{{ .vela-core.test.k8s.registry }}/{{ .vela-core.test.k8s.repository }}:{{ .vela-core.test.k8s.tag }}"
- "{{ .vela-core.admissionWebhooks.patch.image.registry }}/{{ .vela-core.admissionWebhooks.patch.image.repository }}:{{ .vela-core.admissionWebhooks.patch.image.tag }}"
- "{{ .vela-core.fluxcd.kustomize_controller.image.registry }}/{{ .vela-core.fluxcd.kustomize_controller.image.repository }}:{{ .vela-core.fluxcd.kustomize_controller.image.tag }}"
- "{{ .vela-core.fluxcd.image_automation_controller.image.registry }}/{{ .vela-core.fluxcd.image_automation_controller.image.repository }}:{{ .vela-core.fluxcd.image_automation_controller.image.tag }}"
- "{{ .vela-core.fluxcd.image_reflector_controller.image.registry }}/{{ .vela-core.fluxcd.image_reflector_controller.image.repository }}:{{ .vela-core.fluxcd.image_reflector_controller.image.tag }}"
- "{{ .vela-core.fluxcd.kustomize_controller.image.registry }}/{{ .vela-core.fluxcd.kustomize_controller.image.repository }}:{{ .vela-core.fluxcd.kustomize_controller.image.tag }}"
