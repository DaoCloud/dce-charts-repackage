- "{{ .vela-core.global.imageRegistry }}/{{ .vela-core.global.repository }}:{{ .vela-core.global.tag }}"
- "{{ .vela-core.multicluster.clusterGateway.image.registry }}/{{ .vela-core.multicluster.clusterGateway.image.repository }}:{{ .vela-core.multicluster.clusterGateway.image.tag }}"
- "{{ .vela-core.test.app.registry }}/{{ .vela-core.test.app.repository }}:{{ .vela-core.test.app.tag }}"
- "{{ .vela-core.test.k8s.registry }}/{{ .vela-core.test.k8s.repository }}:{{ .vela-core.test.k8s.tag }}"
- "{{ .vela-core.admissionWebhooks.patch.image.registry }}/{{ .vela-core.admissionWebhooks.patch.image.repository }}:{{ .vela-core.admissionWebhooks.patch.image.tag }}"
