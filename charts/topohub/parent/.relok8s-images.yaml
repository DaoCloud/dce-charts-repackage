- "{{ .topohub.image.registry }}/{{ .topohub.image.repository }}:{{ .topohub.image.tag }}"
- "{{ .topohub.httpServer.image.registry }}/{{ .topohub.httpServer.image.repository }}:{{ .topohub.httpServer.image.tag }}"
