---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  name: sshstatuses.topohub.infrastructure.io
spec:
  group: topohub.infrastructure.io
  names:
    kind: SSHStatus
    listKind: SSHStatusList
    plural: sshstatuses
    singular: sshstatus
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.basic.clusterName
      name: CLUSTERNAME
      type: string
    - jsonPath: .status.healthy
      name: HEALTHY
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            properties:
              basic:
                description: SSHBasicInfo incluse SSH connection basic info
                properties:
                  clusterName:
                    type: string
                  subnetName:
                    description: SubnetName is the name of the subnet this host belongs
                      to
                    type: string
                required:
                - clusterName
                type: object
              healthy:
                type: boolean
              info:
                additionalProperties:
                  type: string
                type: object
              lastUpdateTime:
                type: string
            required:
            - basic
            - healthy
            - info
            - lastUpdateTime
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
