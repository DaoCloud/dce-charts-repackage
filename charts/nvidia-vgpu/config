export USE_OPENSOURCE_CHART=false

# must
export REPO_URL=https://project-hami.github.io/HAMi/
export REPO_NAME=hami
export CHART_NAME=hami
export VERSION=2.0.0

# pr, issue, none
export UPGRADE_METHOD=pr
export UPGRADE_REVIWER=lengrongfu
export TEST_ASSIGNER=lengrongfu

# optional, for wrapper chart
export CUSTOM_SHELL=custom.sh
# push to daocloud repo
export DAOCLOUD_REPO_PROJECT=addon

export NO_TRIVY=true

# nvidia-vgpu chart version
export CUSTOM_VERSION=${VERSION}+3
