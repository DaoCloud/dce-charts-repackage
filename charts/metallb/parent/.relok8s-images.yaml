- "{{ .metallb.prometheus.rbacProxy.registry }}/{{ .metallb.prometheus.rbacProxy.repository }}:{{ .metallb.prometheus.rbacProxy.tag }}"
- "{{ .metallb.controller.image.registry }}/{{ .metallb.controller.image.repository }}:{{ .metallb.controller.image.tag }}"
- "{{ .metallb.speaker.image.registry }}/{{ .metallb.speaker.image.repository }}:{{ .metallb.speaker.image.tag }}"
- "{{ .metallb.speaker.frr.image.registry }}/{{ .metallb.speaker.frr.image.repository }}:{{ .metallb.speaker.frr.image.tag }}"